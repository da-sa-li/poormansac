[pytest]
# --strict-markers turns a typo'd or unregistered marker into an error instead
# of a silent no-op, so the thermodynamics selection below can't quietly match
# nothing.
addopts = --strict-markers
markers =
    thermodynamics: pure thermodynamic calculations in calc.py (run by the Thermodynamik CI job)
